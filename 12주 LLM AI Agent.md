# [강의 요약] LLM 기반 AI 에이전트(AI Agent)와 MCP 개요 및 활용



---

## Section 1. AI 에이전트와 MCP 개요

### 1. AI 에이전트(AI Agent)의 정의 및 한계
* **에이전트(Agent)**: 환경을 지각(Perceive)하고, 그 지각에 따라 행동(Act)하는 모든 주체를 의미합니다.
* **AI 에이전트**: 인공지능 기술을 활용하여 환경을 지각하고, 합리적인 판단을 통해 주어진 목표를 달성하기 위한 행동을 수행하는 시스템입니다.
* **이전 LLM의 한계 ("갇혀 있는 천재")**: 
  * 매우 똑똑하지만 '학습된 데이터'에만 갇혀 있습니다.
  * 실시간 정보 접근 불가, 로컬 파일 시스템 제어 불가, 외부 툴 및 API와의 단절이라는 한계를 가집니다.

### 2. MCP(Model Context Protocol)의 등장 배경
* **기존 문제점**: 새로운 서비스나 데이터 소스를 LLM에 연결할 때마다 매번 별도의 커스텀 API 통합 코드를 중복 개발해야 하는 파편화 문제가 존재했습니다.
* **MCP 개요**: 앤스로픽(Anthropic)이 발표한 오픈 소스 프로토콜로, AI 모델과 외부 데이터 소스/도구 간의 안전하고 표준화된 연결을 지원하는 강력한 개방형 표준 규격입니다.
* **MCP 아키텍처 구조**:
  * **MCP 호스트(Host)**: 사용자가 상호작용하는 LLM 애플리케이션 (예: Claude Desktop, claude.ai 등).
  * **MCP 서버(Server)**: 호스트의 요청에 따라 실제 기능이나 데이터를 제공하는 경량 프로그램.
  * **로컬/원격 연결**: 파일 시스템, 브라우저, GitHub, Postgres 등 외부 도구와 데이터를 표준화된 방식으로 안전하게 중복 없이 연결합니다.

---

## Section 2. 클로드 커넥터(Claude Connector) 개요

### 1. 클로드 커넥터의 정의
* **정의**: 클로드 내부에서 사용되는 MCP를 기반으로 설계된 데이터 통합 레이어입니다.
* **역할**: 사용자가 자신의 컴퓨터 환경(로컬 시스템)이나 엔터프라이즈 데이터 인프라를 클로드와 다이렉트로 연결하여 상호작용할 수 있도록 돕는 다리 역할을 수행합니다.

### 2. 클로드 커넥터의 장점
* **동적 맥락 확장**: 고정된 컨텍스트 윈도우의 한계를 넘어, 필요한 순간에 필요한 로컬/클라우드 데이터만 스마트하게 호출하여 비용과 성능을 최적화합니다.
* **표준화된 통합**: 개별 외부 도구마다 API 연동 코드를 복잡하게 짤 필요 없이, 단일화된 MCP 규격에 맞추어 플러그 앤 플레이 방식으로 확장할 수 있습니다.
* **강력한 보안성**: 사용자가 승인한 범위와 권한 내에서만 외부 데이터에 접근하므로 데이터 유출 위험을 효과적으로 차단합니다.

---

## Section 3. 클로드 커넥터 설치 및 실무 활용

### 1. 로컬 환경 연결 및 기본 전제 조건
* **Claude Desktop 활용**: 로컬 PC의 파일 시스템이나 로컬 데이터베이스를 제어하기 위해 클로드 데스크톱 앱을 허브로 삼아 MCP 서버를 구동합니다.
* **구성 파일 설정**: 일반적으로 호스트의 최상위 폴더나 사용자 환경 설정 경로 내의 `claude_desktop_config.json` 파일을 수정하여 필요한 MCP 서버 리스트와 실행 명령을 등록합니다.

### 2. 클로드 인 크롬(Claude in Chrome) 확장 프로그램 활용
웹 브라우저 상에서 클로드 AI의 능력을 즉각적으로 연동하여 생산성을 높일 수 있는 도구입니다.

* **설치 및 연동 4단계**:
  1. **확장 프로그램 추가**: Chrome 웹 스토어에 방문하여 "Claude"를 검색한 후, 'Chrome에 추가(Add to Chrome)' 버튼을 클릭해 설치합니다.
  2. **아이콘 고정**: 브라우저 우측 상단의 퍼즐 조각(확장 프로그램) 아이콘을 클릭한 뒤, "Claude" 옆의 압정 아이콘을 눌러 툴바에 고정합니다.
  3. **로그인 및 권한 부여**: 고정된 Claude 아이콘을 누르고 자신의 Claude 계정으로 로그인한 후, 브라우저 내용과 상호작용할 수 있도록 필요한 접근 권한을 허용합니다.
  4. **사이드 패널 활성화**: 툴바의 클로드 아이콘을 클릭하면 브라우저 우측에 'Claude 사이드 패널'이 열리며, 현재 보고 있는 웹페이지의 콘텐츠 요약, 번역, 질의응답 등의 작업을 화면 이동 없이 실시간으로 수행할 수 있습니다.
